# OTA Two-Stage Design Playbook

这份文档是 `ota-2stage-design-playbook/SKILL.md` 的阅读版说明，目的是把这次两级 OTA 设计过程中沉淀下来的经验，整理成一套可复用的方法。

适用对象：

- 需要在本仓库中设计或调试 1.8V 两级 Miller OTA 的人
- 需要用 gm/ID + HSPICE 扫描，而不是靠猜参数的人
- 关心大信号波形是否正常，而不只是 AC 指标的人

---

## 1. 这套方法解决什么问题

它主要解决以下几类问题：

- 增益不够
- 相位裕度不够
- CMRR / PSRR 不够
- `SR+` / `SR-` 不合适
- unity-follower 大信号波形异常
- 参数很多，不知道先调谁、后调谁
- 电路能跑，但结果不稳定、不可复用

这套方法的核心目标不是“碰巧调出一个能看的点”，而是：

- 让设计过程可解释
- 让参数来源有证据
- 让后续复用时知道优先级和边界条件

---

## 2. 本方法对应的 OTA 拓扑

当前总结的对象是本项目里的两级 OTA：

- 一级：PMOS 差分对
- 一级负载：NMOS 电流镜负载
- 尾电流源：PMOS
- 二级：NMOS 共源放大级
- 二级负载：PMOS 电流源负载
- 补偿：`Rz + Cc` 串联 Miller 补偿

对应实现主要在：

- `references/simulate_2stage_ota.py`
- `references/netlist/`

---

## 3. 最重要的设计原则

### 原则 A：先保证 DC，再看 AC，再看 transient

如果 DC 工作点不对：

- AC 没意义
- CMRR / PSRR 没意义
- transient 也不能信

所以每次改参数后，第一件事不是看波形漂不漂亮，而是先问：

- 所有关键管子是否还在线性区/饱和区的正确状态？
- `vds > vdsat` 是否成立？
- 输出静态点是否合理？

### 原则 B：参数不能靠猜

本项目里用户明确要求：

- 参数优先由 gm/ID 决定
- 不够时用扫描决定
- 不能凭经验拍一个值就当结论

因此正确做法是：

1. gm/ID 给初始点
2. HSPICE 扫描做 refinement
3. 只保留有仿真证据支持的参数

### 原则 C：大信号波形要单独看

一个 OTA 可能：

- 增益很好
- PM 很高
- UGF 也正常

但 unity-follower 大信号波形仍然很差。

所以必须单独看：

- `Vout`
- 内部节点 `n2`
- 是否具备正常的 rise-hold-fall 三段行为

### 原则 D：扫描时优先用 `.alter` 批量扫

如果每扫一个点都重新开一次 HSPICE，时间会大量浪费在 simulator 启停上。

所以建议：

- 用 `.alter` 一次跑多个点
- 优先写 batch sweep 脚本

---

## 4. 推荐设计顺序

推荐严格按这个顺序进行。

### 第一步：先判断拓扑值不值得继续调

如果拓扑本身不够好，就不要在上面浪费很多时间。

本项目的经验是：

- baseline 5T OTA 开环增益太低
- 即使继续调，也很难满足较高指标
- 所以应尽早切换到两级 Miller OTA

### 第二步：先确认测试平台是不是合理

很多“电路问题”其实是测试条件不合理。

尤其 transient：

- 输入摆幅过大
- 输入共模超出有效范围
- 输出目标本来就达不到

都可能让你误以为 OTA 本身有严重问题。

因此先确认：

- 输入范围合理
- 负载真实
- unity-follower 连接正确

### 第三步：用 gm/ID 选初始尺寸

不要从随便猜的 W/L 开始。

这里推荐：

- 输入对、镜像负载、尾电流源、二级 NMOS、二级 PMOS 都先走一遍 gm/ID
- 得到第一个“物理上合理”的尺寸起点

gm/ID 的作用不是一步到位，而是避免你从错误起点开始。

### 第四步：先做 DC gate

每组尺寸和偏置都先过 DC：

- `.op`
- 器件区间检查
- 输出偏置检查
- 必要时扫 `VBP2`

### 第五步：再做 AC 验证

确认：

- 开环增益
- UGF
- PM
- CMRR
- PSRR

### 第六步：最后做 transient / slew 调整

这一步才开始动：

- `Cc`
- `Rz`
- stage-2 drive
- 负载条件

并且一定同时看：

- 输出波形
- 内部节点 `n2`
- rise / hold / fall
- `SR+`
- `SR-`

---

## 5. 各类管子的尺寸选择策略

### 5.1 输入对 `XM1/XM2`

目标：

- 提供足够 gm
- 保证一级速度
- 不要太弱导致后面补偿很难做

建议：

- `L` 不要过长，否则 gm 会掉太多
- 但也不要短到增益明显不够
- 通常比镜像负载和尾源短一些

本项目最终：

- `L_IN = 0.18 um`
- `NFIN_IN = 20`
- `M_IN = 1`

这说明策略是：

- 输入对优先保证 gm
- 不要一开始就用很大 `m`
- 除非确定一级 gm 明显不足

### 5.2 镜像负载 `XM3/XM4`

目标：

- 提高一级输出电阻
- 实现单端输出节点 `n2`
- 不要把 `n2` 压得太死

建议：

- `L` 用得比输入对长
- 尺寸适中，不要盲目做太大

本项目最终：

- `L_LOAD = 0.24 um`
- `NFIN_LOAD = 8`
- `M_LOAD = 1`

### 5.3 尾电流源 `XM5`

目标：

- 给输入对提供稳定尾电流
- 保证共模行为合理
- 不浪费头间裕量

建议：

- 用较长沟道，提高电流源品质
- 尺寸只要够电流即可，不要过大

本项目最终：

- `L_TAIL = 0.24 um`
- `NFIN_TAIL = 12`
- `M_TAIL = 1`

### 5.4 二级 NMOS `XM6`

目标：

- 提供输出下拉能力
- 决定很大一部分 `SR-`
- 决定二级驱动强度

这是最敏感的器件之一。

关键经验：

- 如果它太弱，输出上升慢、几乎没有 hold
- 如果它太强，`SR-` 会很大
- 不能只根据某一个指标单独调它

本项目最终保留：

- `L_STAGE2 = 0.24 um`
- `NFIN_STAGE2 = 11`
- `M_STAGE2 = 4`

注意：这里用了 `m=4`，说明大驱动能力是通过并联实现的，而不是一味增大 `NFIN`。

### 5.5 二级 PMOS 负载 `XM7`

目标：

- 提供输出上拉能力
- 配合 `XM6` 决定输出静态工作点
- 影响上升段和 DC 平衡

关键经验：

- 它不能和 `XM6` 分开单独理解
- `XM6/XM7` 必须当作一对系统来调
- 如果 `XM7` 太强，输出静态点会被顶高

本项目最终保留：

- `L_P2 = 0.24 um`
- `NFIN_P2 = 15`
- `M_P2 = 4`

---

## 6. 为什么要用 `m`，而不是只调 `NFIN`

这是这次项目里很重要的一条经验。

当 gm/ID 算出来的有效宽度比较大时：

- 不要强行把所有宽度都塞到单个器件的 `NFIN` 上
- 优先考虑用并联倍数 `m`

理由：

- 更符合实际可实现的器件组织方式
- 更容易保持参数扫描的连续性
- 避免单管过宽导致的奇怪行为

所以：

- 输入级如果还不算太大，可以先不加 `m`
- 二级驱动如果需要明显增强，优先用 `m`

---

## 7. 偏置选择策略

### `VBP1`

它主要决定尾电流源。

策略：

- 从 gm/ID 所需电流反推
- 再用 DC 检查确认 headroom

### `VBP2`

它非常敏感，是整个设计里最需要小步扫描的偏置之一。

本项目的关键经验：

- `VBP2_DROP = 0.528 V` 是一个窄窗口可用点
- 再往上调一点点，输出静态点就会明显跳高

因此：

- 一旦碰 `VBP2`，必须先做细扫描
- 不能因为 transient 某个点看起来更好，就直接采用
- 如果那个点 DC 不成立，就不能用

---

## 8. 补偿参数怎么选

### `Cc`

`Cc` 太小：

- 内部节点 `n2` 可能塌陷得很厉害
- 输出容易出现异常 overshoot 或非正常大信号行为

`Cc` 太大：

- 输出上升很慢
- 在输入高电平期间来不及到位
- 波形几乎没有 hold 段

因此 `Cc` 不能凭印象选，只能扫。

### `Rz`

`Rz` 不只是“把 PM 调顺”的参数。

它还会影响：

- compensation zero
- 大信号回落速度
- `SR-`

本项目最终：

- `Cc = 3.5p`
- `Rz = 6.8k`

这组值的意义是：

- 波形正常
- overshoot 很小
- `SR+` 保持在目标附近
- `SR-` 虽然仍偏大，但比之前大幅下降

---

## 9. 本项目里最关键的 transient 经验

### 问题 1：波形最初为什么不正常

一开始的问题不是单一原因，而是叠加造成：

- 测试输入范围不够合理
- `n2` 在大信号过程中塌陷严重
- 补偿初值不合适
- 二级驱动不足

### 问题 2：为什么后来“没有 hold，且上升很慢”

原因是：

- 为了抑制异常波形，把 `Cc` 加太大了
- 但二级驱动没有同步增强
- 导致输出在高电平期间大部分时间都耗在慢慢爬升

### 问题 3：最后为什么 `SR-` 很大

因为为了恢复正常的 rise-hold-fall：

- 二级驱动被增强到了 `m6=m7=4`
- 这样上升段和 hold 恢复了
- 但下降时 `XM6` 过强，输出放电非常快

最后的折中是：

- 不再继续破坏 DC 去硬压 `SR-`
- 而是通过重新调整 `Cc/Rz` 把 `SR-` 从更高的值压低到可接受折中

---

## 10. 在设计过程中优先做什么

优先级建议如下：

1. **确认拓扑是不是值得做**
2. **确认测试平台是不是合理**
3. **gm/ID 给初始尺寸**
4. **DC gate**
5. **AC gate**
6. **transient gate**
7. **批量扫描 refinement**
8. **根据真实优先级选最终点**

---

## 11. 不能做什么

这些是本项目已经证明会浪费时间或导致错误结论的做法。

### 不能做的事 1：DC 没过就解读 AC / transient

这是最容易犯的错误。

### 不能做的事 2：靠感觉猜 `Cc`、`Rz`、`VBP2`

这些参数都非常敏感，必须扫描。

### 不能做的事 3：看到 transient 某个点“更漂亮”就直接采用

如果那个点的静态工作点坏了，它就不是设计解。

### 不能做的事 4：一味加大 `Cc` 压问题

这样常常只是把“异常 overshoot”换成“慢到没有 hold 的波形”。

### 不能做的事 5：强行把所有有效宽度都塞进 `NFIN`

二级尺寸需要大时，应优先考虑 `m`。

---

## 12. 能做什么

下面这些是推荐且有效的手段：

- 用 gm/ID 做初始尺寸
- 用 `.alter` 批量扫参数
- 单独观察 `n2`
- 用 `m` 扩展二级有效宽度
- 细扫 `VBP2`
- 扫 `Cc/Rz`
- 扫 stage-2 drive
- 在必要时写专用 sweep 脚本提高效率

---

## 13. 当前项目最后保留的设计点

当前保留的最终设计点是：

- `VDD = 1.8 V`
- `VCM = 0.9 V`
- `VBP1_DROP = 0.600 V`
- `VBP2_DROP = 0.528 V`
- `L_IN = 0.18 um`, `NFIN_IN = 20`, `M_IN = 1`
- `L_LOAD = 0.24 um`, `NFIN_LOAD = 8`, `M_LOAD = 1`
- `L_TAIL = 0.24 um`, `NFIN_TAIL = 12`, `M_TAIL = 1`
- `L_STAGE2 = 0.24 um`, `NFIN_STAGE2 = 11`, `M_STAGE2 = 4`
- `L_P2 = 0.24 um`, `NFIN_P2 = 15`, `M_P2 = 4`
- `Cc = 3.5 pF`
- `Rz = 6.8 kOhm`
- `CL = 8 pF`

最终验证大致为：

- Gain ≈ `93.85 dB`
- UGF ≈ `7.71 MHz`
- PM ≈ `98.77 deg`
- CMRR ≈ `88.8 dB`
- PSRR+ ≈ `91.1 dB`
- `SR+ ≈ 5 V/us`
- `SR- ≈ 12 V/us`
- unity-follower 波形恢复为正常的 rise-hold-fall

---

## 14. 建议怎么使用这套 playbook

如果以后复用，推荐按下面方式使用：

1. 先复制当前两级 OTA 网表结构
2. 替换目标指标和负载条件
3. 用 gm/ID 重新算初始尺寸
4. 先把 DC 跑通
5. 再扫 `VBP2`
6. 再扫 `Cc/Rz`
7. 最后再看是否需要加强或减弱二级驱动

这样做的好处是：

- 每一步都有明确目的
- 不会一上来就在很多参数上乱调
- 后续复用成本低

---

## 15. 对应文件

- Playbook skill：`SKILL.md`
- 阅读说明：`README.md`
- 两级 OTA 实现快照：`references/simulate_2stage_ota.py`
- 网表模板快照：`references/netlist/`
- gm/ID 参考实现快照：`references/design_gmoverid.py`
- 设计报告：`references/2stage_ota_design_report.md`
- 仿真报告：`references/2stage_ota_simulation_report.md`

现在这个打包版本还额外包含了 `assets/`，里面放的是这次设计过程中实际用到的批量扫描脚本：

- `assets/hspice_common.py`：公共 HSPICE 启动、日志解析、工程单位解析工具
- `assets/sweep_vbp2_batch.py`：单次 HSPICE 启动完成 `VBP2` 的 `.alter` 批扫
- `assets/sweep_compensation_batch.py`：扫描 `Cc/Rz`
- `assets/sweep_vbp2_cc_batch.py`：联合扫描 `VBP2 + Cc`
- `assets/sweep_stage2_drive_batch.py`：扫描二级驱动强度 `m_stage2/m_p2`
- `assets/sweep_srneg_batch.py`：定位 `SR-` 对二级尺寸的敏感性
- `assets/sweep_cl_batch.py`：评估 `CL` 对 slew 的影响
- `assets/sweep_comp_srneg_batch.py`：在强驱动点附近重新扫描 `Cc/Rz` 以压低 `SR-`

这些脚本的定位是“方法示例”和“批扫模板”，不是说换一个工艺后还能原样直接用。

说明：

- 为了便于对外发布，这个 skill 已经把核心参考文件复制到 `references/` 下。
- 这次也把 sweep 资产一起复制到了 `assets/` 下，便于复现设计过程。
- 文档中的路径已经改为相对路径，不再依赖本地绝对目录。

---

## 16. 工艺迁移时应该怎么做

这套设计不是只改一个 `.lib` 路径就能直接迁移成功。

因为它同时依赖：

- 当前 HSPICE model deck
- 当前器件接口名
- 当前 FinFET 几何参数语义
- 当前 gm/ID 数据

所以建议按下面顺序迁移。

### 16.1 先替换 model library，再做最小可运行验证

当前示例默认使用类似这样的库：

- `.lib '.../12sfe_spice_v1p2_rev0_usage.lib' tt_mos_varactor`

如果用户不是这个库，需要先检查：

1. `.lib` 路径是否正确
2. corner 名称是否还是 `tt_mos_varactor`
3. `n18_ckt / p18_ckt` 这些器件名是否仍然存在
4. 新库是否支持当前网表里用到的参数名

正确做法不是一上来就调 `Cc/Rz`，而是：

- 先跑一个最小 `.op` deck
- 确认模型能正常调用
- 再接入完整 OTA 网表

### 16.2 gm/ID 表必须重建

gm/ID 数据是工艺相关的，不可直接照搬。

一旦换工艺或换模型库，需要重新生成：

- `gm/Id`
- `gm/gds`
- `ft`
- 电流密度范围
- 可用工作区间

然后再重新得到：

- 输入对尺寸
- 负载尺寸
- 尾电流源尺寸
- 二级尺寸
- 偏置电流等级

也就是说，旧设计里的 `NFIN`、`L`、`VBP1`、`VBP2` 都只能当参考，不是迁移后的答案。

### 16.3 偏置必须重扫，不能直接沿用

这个项目里最终保留：

- `VBP1_DROP = 0.600 V`
- `VBP2_DROP = 0.528 V`

这些值只在当前供电、当前阈值、当前头间裕量条件下成立。

换工艺后必须重新做：

- `.op`
- 输出静态点检查
- `VBP2` 细扫
- 必要时输入共模扫描

只要工艺变了，就不要假设这两个 bias 还能直接用。

### 16.4 补偿参数也必须重新扫

工艺变了以后：

- 寄生电容会变
- 输出电阻会变
- 极点零点位置会变
- stage-2 的驱动/负载平衡会变

所以 `Cc = 3.5p`、`Rz = 6.8k` 也不能直接当作迁移结果。

正确做法仍然是：

1. 先 gm/ID 初始尺寸
2. 再 DC gate
3. 再 AC gate
4. 最后重新扫 `Cc/Rz` 和 stage-2 drive

---

## 17. 这个设计为什么说是基于 FinFET 工艺

因为当前网表和脚本都明显采用了 FinFET 风格的器件参数，而不是传统平面 CMOS 的纯 `W/L` 表达。

最直接的证据就是网表里用了：

- `nfin`
- `nf`
- `m`

其中 `nfin` 是这套流程里最关键的 FinFET 特征之一。

### 17.1 `NFIN` 不是普通连续宽度，而是离散几何量

在 FinFET 工艺里，驱动能力通常不是靠连续变化的宽度去调，而是和 fin 数直接相关。

这意味着：

- 尺寸不是完全连续可调的
- `NFIN` 改 1，电流和电容都可能明显变化
- 精细调节时常常需要 `m` 一起配合

### 17.2 为什么这次强调优先用 `m`

本项目的一个关键经验就是：

- 不要把所有额外驱动都硬塞给 `NFIN`
- 当需要更大有效宽度时，优先考虑并联倍数 `m`

原因有三点：

1. 更符合 FinFET 中较大驱动器件的实现方式
2. 避免单管 `NFIN` 堆得过大
3. 扫描时更平滑、更容易比较

所以最终二级用了：

- `NFIN_STAGE2 = 11`, `M_STAGE2 = 4`
- `NFIN_P2 = 15`, `M_P2 = 4`

这不是随意写法，而是这次设计方法的一部分。

### 17.3 `nf` 和 `m` 不能想当然地互换

很多人会把 `nf`、`m`、`NFIN` 都当成“等效变大”的旋钮，但在不同 PDK 里它们的物理意义并不相同。

通常需要分别确认：

- `nf` 是不是单器件内部 fingers
- `m` 是不是实例并联复制
- `w` 到底是不是单 fin 宽度、等效宽度，还是别的定义

如果不先搞清这些含义，迁移时很容易得到表面能跑、但尺寸含义已经错掉的网表。

### 17.4 如果迁到平面 CMOS，要保留流程，不要硬搬参数

如果目标工艺不是 FinFET，而是普通 planar CMOS，那么正确做法不是继续沿用 `NFIN` 思路，而是：

- 改回 width-based sizing
- 用新的 gm/ID 数据重算 `W/L`
- 重做 DC / AC / transient 扫描
- 重新选 compensation

要迁移的是这套方法：

- 先拓扑
- 再 DC
- 再 gm/ID 初值
- 再 batch sweep refinement

而不是把当前这些 `NFIN/m/bias/Cc/Rz` 原样搬过去。

如果你要把这套方法用于新的 OTA 目标，建议从 `SKILL.md` 开始看；如果你只想快速理解整体思路，看这份 README 就够了。

